__pycache__/
*.pyc
.pytest_cache/
.coverage
.tox/
node_modules/
dist/
.vite/
*.tsbuildinfo
.DS_Store
config/plugins.json
config/copyparty.json
config/pihealth_update.json
# static/v2/ is the built web UI bundle. It is committed (not ignored) so that
# `git pull` ships a ready-to-serve UI to hosts without a Node toolchain.
# Rebuild with `cd frontend && npm run build:publish` and commit the result.
*.tar.zst
