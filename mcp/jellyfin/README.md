# Jellyfin MCP Server

Endpoints returning system info, libraries, sessions, and scheduled tasks for Ops-Copilot.

Env vars: `JELLYFIN_BASE_URL`, `JELLYFIN_API_KEY`/`_FILE`, `JELLYFIN_USER_ID`/`_FILE`, optional `JELLYFIN_HTTP_TIMEOUT`.
