# SABnzbd MCP Server

FastAPI service mapping the SABnzbd API to MCP tools (`get_status`, `get_queue`, `get_warnings`).

Env vars: `SABNZBD_BASE_URL`, `SABNZBD_API_KEY`/`_FILE`, optional `SABNZBD_HTTP_TIMEOUT`.
